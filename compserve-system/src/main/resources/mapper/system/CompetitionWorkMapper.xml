<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.iflytek.system.mapper.CompetitionWorkMapper">
    
    <resultMap type="CompetitionWork" id="CompetitionWorkResult">
        <result property="workId"    column="work_id"    />
        <result property="participationId"    column="participation_id"    />
        <result property="fileName"    column="file_name"    />
        <result property="filePath"    column="file_path"    />
        <result property="fileType"    column="file_type"    />
        <result property="fileSize"    column="file_size"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectCompetitionWorkVo">
        select work_id, participation_id, file_name, file_path, file_type, file_size, create_time from competition_works
    </sql>

    <select id="selectCompetitionWorkList" parameterType="CompetitionWork" resultMap="CompetitionWorkResult">
        <include refid="selectCompetitionWorkVo"/>
        <where>  
            <if test="participationId != null "> and participation_id = #{participationId}</if>
            <if test="fileName != null  and fileName != ''"> and file_name like concat('%', #{fileName}, '%')</if>
            <if test="fileType != null  and fileType != ''"> and file_type = #{fileType}</if>
        </where>
        order by create_time desc
    </select>
    
    <select id="selectCompetitionWorkByWorkId" parameterType="Long" resultMap="CompetitionWorkResult">
        <include refid="selectCompetitionWorkVo"/>
        where work_id = #{workId}
    </select>
        
    <insert id="insertCompetitionWork" parameterType="CompetitionWork" useGeneratedKeys="true" keyProperty="workId">
        insert into competition_works
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="participationId != null">participation_id,</if>
            <if test="fileName != null">file_name,</if>
            <if test="filePath != null">file_path,</if>
            <if test="fileType != null">file_type,</if>
            <if test="fileSize != null">file_size,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="participationId != null">#{participationId},</if>
            <if test="fileName != null">#{fileName},</if>
            <if test="filePath != null">#{filePath},</if>
            <if test="fileType != null">#{fileType},</if>
            <if test="fileSize != null">#{fileSize},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateCompetitionWork" parameterType="CompetitionWork">
        update competition_works
        <trim prefix="SET" suffixOverrides=",">
            <if test="participationId != null">participation_id = #{participationId},</if>
            <if test="fileName != null">file_name = #{fileName},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="fileType != null">file_type = #{fileType},</if>
            <if test="fileSize != null">file_size = #{fileSize},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where work_id = #{workId}
    </update>

    <delete id="deleteCompetitionWorkByWorkId" parameterType="Long">
        delete from competition_works where work_id = #{workId}
    </delete>

    <delete id="deleteCompetitionWorkByWorkIds" parameterType="String">
        delete from competition_works where work_id in 
        <foreach item="workId" collection="array" open="(" separator="," close=")">
            #{workId}
        </foreach>
    </delete>

    <delete id="deleteCompetitionWorkByParticipationId" parameterType="Long">
        delete from competition_works where participation_id = #{participationId}
    </delete>
</mapper>
