# 本机联调用 Profile：数据库地址和库名已按讯飞杯开发库预设。
# 密码与讯飞杯 Token 密钥必须来自 IDEA 或启动进程的环境变量，切勿提交真实凭据。
spring:
  datasource:
    druid:
      master:
        url: ${DB_URL:jdbc:mysql://117.72.79.75:3306/compserve?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8}
        username: ${DB_USERNAME:root}
        password: ${DB_PASSWORD}

# 讯飞杯登录使用独立 Token；至少 64 字节随机字符串。
xfc:
  token:
    secret: ${XFC_TOKEN_SECRET}
